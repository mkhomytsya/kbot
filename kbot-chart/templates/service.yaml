apiVersion: v1
kind: Service
metadata:
  name: {{ include "kbot-chart.fullname" . }}
  labels:
    {{- include "kbot-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kbot-chart.selectorLabels" . | nindent 4 }}
